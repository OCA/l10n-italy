<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record model="ir.ui.view" id="view_settlement_line_tree_extended">
        <field name="name">Settlement lines extended</field>
        <field name="model">sale.commission.settlement</field>
        <field name="inherit_id" ref="sale_commission.view_settlement_form"/>
        <field name="arch" type="xml">
            <field name="date" position="after">
                <field name="partner"/>
                <field name="imponibile"/>
                <field name="tot_invoice"/>
                <field name="invoice_state"/>
                <field name="payment_date"/>
            </field>
        </field>
    </record>
</odoo>
