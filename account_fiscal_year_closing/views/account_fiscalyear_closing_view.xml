<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright 2016 Antonio Espinosa <antonio.espinosa@tecnativa.com>
     License AGPL-3.0 or later (http://www.gnu.org/licenses/agpl). -->

<odoo>

    <record id="view_account_fiscalyear_closing_tree" model="ir.ui.view">
        <field name="model">account.fiscalyear.closing</field>
        <field name="arch" type="xml">
            <tree string="Fiscal year closing">
                <field name="name"/>
                <field name="company_id" groups="base.group_multi_company"/>
                <field name="date_start"/>
                <field name="date_end"/>
                <field name="date_opening"/>
                <field name="state"/>
                <field name="calculation_date"/>
            </tree>
        </field>
    </record>

    <record id="view_account_fiscalyear_closing_form" model="ir.ui.view">
        <field name="model">account.fiscalyear.closing</field>
        <field name="arch" type="xml">
            <form string="Fiscal year closing">
                <header>
                    <field name="move_ids" invisible="1"/>
                    <field name="move_line_ids" invisible="1"/>
                    <button name="button_calculate" states="draft"
                            type="object" string="Calculate" class="oe_highlight" />
                    <button name="button_recalculate" states="calculated"
                            type="object" string="Recalculate"/>
                    <button name="button_post"
                            type="object" string="Post moves" class="oe_highlight"
                            attrs="{'invisible': ['|', ('move_config_ids', '=', []), ('state', 'not in', ['calculated'])]}"/>
                    <button name="button_recover" states="cancelled"
                            type="object" string="Draft"/>
                    <button name="button_open_moves" states="calculated,posted"
                            type="object" string="Show moves"/>
                    <button name="button_open_move_lines" states="calculated,posted"
                            type="object" string="Show move lines"/>
                    <button name="button_cancel" states="calculated,posted"
                            type="object" string="Cancel"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,calculated,posted,cancelled"
                           statusbar_colors="{'cancelled': 'red', 'posted': 'blue'}"/>
                </header>
                <sheet>
                    <h1>
                        <label string="Fiscal closing"/>
                        <field name="name" class="oe_inline"/>
                    </h1>
                    <group>
                        <group>
                            <field name="date_start"/>
                            <field name="date_end"/>
                            <field name="date_opening"/>
                        </group>
                        <group>
                            <field name="check_draft_moves"/>
                            <field name="company_id" groups="base.group_multi_company"/>
                        </group>
                    </group>
                    <group attrs="{'invisible': [('calculation_date', '=', False)]}">
                        <group>
                            <field name="calculation_date"/>
                        </group>
                    </group>
                    <group string="Moves configuration">
                        <field name="move_config_ids" nolabel="1" widget="one2many_list"
                               mode="tree,kanban" context="{'default_fyc_id': id}"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_account_fiscalyear_closing_search" model="ir.ui.view">
        <field name="model">account.fiscalyear.closing</field>
        <field name="arch" type="xml">
            <search string="Fiscal year closing">
                <filter string="Draft" domain="[('state','=','draft')]"/>
                <filter string="In process" domain="[('state', '=', 'calculated')]"/>
                <filter string="Posted" domain="[('state', '=', 'posted')]"/>
                <filter string="Cancelled" domain="[('state', '=', 'cancelled')]"/>
                <filter string="Company" domain="[]" context="{'group_by': 'company_id'}" groups="base.group_multi_company"/>
            </search>
        </field>
    </record>


    <record id="action_account_fiscalyear_closing" model="ir.actions.act_window">
        <field name="name">Fiscal year closings</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">account.fiscalyear.closing</field>
        <field name="view_type">form</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="view_account_fiscalyear_closing_tree" />
    </record>

    <menuitem id="menu_account_fiscalyear_closing"
              parent="account.menu_finance_entries" sequence="90"
              name="Fiscal year closings" action="action_account_fiscalyear_closing"/>

</odoo>
